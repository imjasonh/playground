# sshcloud Terraform

GCP resources + `terraform-provider-ko` for platform binaries (`gateway`,
`orchestrator`, `agent`, `api`). Host MIG for Firecracker agents; single
gateway VM in v1.

**Status:** stub. Modules land with the first deployable environment.
