{
  "name": "crane",
  "version": "1.0.0",
  "description": "Yard Crane — Godot tower-crane game exported for the browser",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "npx --yes serve . -p 3000",
    "export": "bash scripts/export.sh"
  }
}
