{
  "name": "mta-subway-live",
  "version": "1.0.0",
  "description": "Realtime NYC subway arrivals, train locations, and service status from the MTA GTFS-realtime feeds — pure client-side, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e",
    "start": "npx --yes serve . -p 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "jest": "^29.7.0"
  }
}
