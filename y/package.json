{
  "name": "y",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply y --local",
    "migrate:remote": "wrangler d1 migrations apply y --remote"
  },
  "dependencies": {
    "@simplewebauthn/server": "^11.0.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
