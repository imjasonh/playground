# Copy to `.dev.vars` for `wrangler dev`. Don't commit `.dev.vars`.
# Generate ADMIN_PASSWORD_HASH with:
#   cargo run --example hash-password -- 'your-admin-password'
ADMIN_PASSWORD_HASH="pbkdf2$100000$<salt-hex>$<hash-hex>"
SESSION_SECRET="<32+ bytes of random hex>"
