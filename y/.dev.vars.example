# Copy to `.dev.vars` for `wrangler dev`. Don't commit `.dev.vars`.
# Generate ADMIN_PASSWORD_HASH with: node scripts/hash-password.mjs <password>
ADMIN_PASSWORD_HASH="pbkdf2$200000$<salt-hex>$<hash-hex>"
SESSION_SECRET="<32+ bytes of random hex>"
