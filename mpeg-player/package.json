{
  "name": "mpeg-canvas-player",
  "version": "1.0.0",
  "private": true,
  "description": "Worker-driven MPEG-TS player with WASM decoding, canvas rendering, and Web Audio",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e",
    "vendor": "node scripts/vendor.js",
    "start": "npx --yes serve . -p 3000"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@seydx/jsmpeg": "1.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
