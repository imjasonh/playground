# Firmware and generated-hardware checks for the inkbot-magsafe e-paper tile.
#
# inkbot-magsafe/ is excluded from the shared Rust job in test.yml because it
# cross-compiles to the bare-metal thumbv7em-none-eabihf target, which the
# generic stable-Rust job cannot build. This workflow also checks the schematic
# contracts, BOM coverage, generated files, and routed PCB.

name: inkbot-magsafe

on:
  push:
    branches: [main]
    paths:
      - "inkbot-magsafe/**"
      - "docs/inkbot-magsafe-bom.csv"
      - "docs/inkbot-magsafe-design.md"
      - ".github/workflows/inkbot-magsafe.yml"
  pull_request:
    paths:
      - "inkbot-magsafe/**"
      - "docs/inkbot-magsafe-bom.csv"
      - "docs/inkbot-magsafe-design.md"
      - ".github/workflows/inkbot-magsafe.yml"
  workflow_dispatch: {}

concurrency:
  group: inkbot-magsafe-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  firmware:
    name: Host tests + ARM build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: inkbot-magsafe
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      # rust-toolchain.toml pins stable and the thumbv7em-none-eabihf target;
      # rustup installs both on the first cargo call.
      - name: Install Rust toolchain
        run: |
          rustup show active-toolchain || rustup toolchain install stable --profile minimal
          rustup component add clippy rustfmt
          rustup target add thumbv7em-none-eabihf

      - name: Format
        run: cargo fmt --all --check

      - name: Clippy (host)
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: Test (host logic)
        run: cargo test --locked

      - name: Test image-layout checker
        run: python3 -m unittest tools/test_check_elf_layout.py

      - name: Qualify target image
        run: |
          classification="$(
            python3 -c \
              'import json; print(json.load(open("production-gates.json"))["classification"])'
          )"
          if [[ "$classification" == "PRODUCTION" ]]; then
            cargo check --locked --no-default-features \
              --target thumbv7em-none-eabihf
          else
            if cargo check --locked --no-default-features \
              --target thumbv7em-none-eabihf; then
              echo "EVT target build succeeded without the explicit inert-image feature"
              exit 1
            fi
          fi

      - name: Clippy (thumbv7em-none-eabihf)
        run: |
          feature_args=()
          classification="$(
            python3 -c \
              'import json; print(json.load(open("production-gates.json"))["classification"])'
          )"
          if [[ "$classification" != "PRODUCTION" ]]; then
            feature_args=(--features bringup-stub)
          fi
          cargo clippy --locked \
            --no-default-features \
            --target thumbv7em-none-eabihf \
            "${feature_args[@]}" \
            -- -D warnings

      - name: Build firmware (thumbv7em-none-eabihf)
        run: |
          feature_args=()
          classification="$(
            python3 -c \
              'import json; print(json.load(open("production-gates.json"))["classification"])'
          )"
          if [[ "$classification" != "PRODUCTION" ]]; then
            feature_args=(--features bringup-stub)
          fi
          elf="${CARGO_TARGET_DIR:-target}/thumbv7em-none-eabihf/release/inkbot-magsafe"
          cargo build --locked --release --no-default-features \
            --target thumbv7em-none-eabihf \
            "${feature_args[@]}"
          python3 tools/check_elf_layout.py \
            "$elf" \
            --flash-start 0x1c000 \
            --flash-end 0x5c000 \
            --ram-start 0x20008000 \
            --ram-end 0x20040000

  hardware:
    name: Schematic contracts + PCB DRC
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: inkbot-magsafe/kicad
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Install KiCad
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            kicad \
            kicad-footprints \
            kicad-symbols

      - name: Check generated schematic
        run: |
          python3 generate_schematic.py
          kicad-cli sch export netlist \
            -o /tmp/inkbot.net \
            inkbot-magsafe.kicad_sch
          git diff --exit-code -- \
            inkbot-magsafe.kicad_sch \
            inkbot-magsafe.kicad_pro

      - name: Check generated placement
        run: INKBOT_PLACE_ONLY=1 python3 route_freerouting.py

      - name: Check fabrication release gates
        run: |
          python3 release_gates.py
          python3 -m unittest test_release_gates.py test_run_drc.py

      - name: Check electrical contracts and BOM
        run: python3 run_erc.py

      - name: Check routed board
        run: python3 run_drc.py

  native-eda:
    name: Native KiCad 9 ERC + DRC
    runs-on: ubuntu-latest
    container:
      image: kicad/kicad:9.0@sha256:e638b79b0321f29395a5b783e94bb9f3c73303e8da15da27b8f5cb4b67a37729
      options: --user root
    defaults:
      run:
        working-directory: inkbot-magsafe/kicad
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Electrical rules
        run: |
          status=0
          kicad-cli sch erc \
            --format report \
            --severity-error \
            --exit-code-violations \
            --output /tmp/inkbot-erc.rpt \
            inkbot-magsafe.kicad_sch || status=$?
          cat /tmp/inkbot-erc.rpt
          exit "$status"

      - name: Board rules and schematic parity
        run: |
          status=0
          kicad-cli pcb drc \
            --format report \
            --severity-error \
            --schematic-parity \
            --exit-code-violations \
            --output /tmp/inkbot-drc.rpt \
            inkbot-magsafe.kicad_pcb || status=$?
          cat /tmp/inkbot-drc.rpt
          exit "$status"
