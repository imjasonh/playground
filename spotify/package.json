{
  "name": "spotify",
  "version": "1.0.0",
  "private": true,
  "description": "Browser Spotify player: login, search, and play with the Web Playback SDK",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "start": "npx --yes serve . -p 3000"
  },
  "engines": {
    "node": ">=20"
  }
}
