#pragma once

// Copy to include/config.h (gitignored) and fill in your values.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// inkbot Worker base URL, no trailing slash.
// Example: "https://inkbot.<account>.workers.dev"
#define INKBOT_BASE_URL "https://inkbot.example.workers.dev"

// Poll interval between image checks.
#define POLL_INTERVAL_MS (60UL * 1000UL)
