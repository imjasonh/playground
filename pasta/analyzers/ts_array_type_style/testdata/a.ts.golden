function f(xs: number[]): number { // want `Array<T> can be written as T[]`
    return xs[0];
}

function g(xs: number[]): number { // OK
    return xs[0];
}

function h(xs: string[]): string { // want `Array<T> can be written as T[]`
    return xs.join(",");
}

type M<T> = Map<string, T>; // OK: Map, not Array
