# want "must enable [observability]" # want "must enable [observability.logs]" # want "must set invocation_logs" # want "must enable [observability.traces]"
name = "off-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2024-11-01"

[observability]
enabled = false
head_sampling_rate = 1

[observability.logs]
enabled = false
invocation_logs = false

[observability.traces]
enabled = false
head_sampling_rate = 1
