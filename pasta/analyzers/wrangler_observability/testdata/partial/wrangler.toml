# want "must enable [observability.logs]" # want "must set invocation_logs" # want "must enable [observability.traces]"
name = "partial-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2024-11-01"

[observability]
enabled = true
head_sampling_rate = 1
