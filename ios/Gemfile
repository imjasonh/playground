source "https://rubygems.org"

# Pin fastlane via bundler so local and CI runs agree. Gemfile.lock is
# committed; re-run `bundle update fastlane` when you intentionally bump.
gem "fastlane", "~> 2.222"
